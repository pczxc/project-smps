# 股票投资管理桌面应用 用户需求说明书
**版本号**：V1.5 最终版（Tauri 2.0 专属）  
**文档日期**：2026-05-15  
**适用技术栈**：**Tauri 2.0 正式版**（Rust后端+系统WebView）+ React 18 + TypeScript + Vite  
**目标用户**：个人散户（Windows系统，每日使用<1小时）  
**文档状态**：已确认，可直接用于开发

---

## 1. 项目概述
### 1.1 项目背景
现有主流股票软件普遍存在功能繁杂、广告多、启动慢、隐私性差等问题，且过度强调盘中实时盯盘，容易干扰有稳定工作的个人投资者的日常工作。本项目采用**Tauri 2.0 正式版**架构，打造一款**轻量化、非打扰式、完全本地**的个人投资管理工具，专注于中长线投资者的核心需求。

### 1.2 架构选型说明：为什么选择 Tauri 2.0
Tauri 2.0 是2026年桌面应用开发的工业级标准，完美匹配本项目所有需求：
- **极致轻量**：使用系统原生WebView，无需打包Chromium，最终安装包仅**8-12MB**
- **内存高效**：后台运行内存占用≤40MB，比Electron低70%以上
- **安全第一**：默认最小权限原则，严格的前后端隔离，Rust内存安全
- **全平台支持**：一键生成Windows/macOS/Linux安装包
- **生态成熟**：2026年企业级应用首选，文档完善，社区活跃
- **开发高效**：前端复用React/Vue生态，后端Rust处理高性能逻辑

### 1.3 核心价值
- **高效筛选**：快速筛选符合条件的股票，建立个人股票池
- **智能提醒**：每日收盘后自动分析股票池，触发条件时给出交易建议
- **精准管理**：自动计算持仓盈亏和资产情况，无需手动统计
- **AI辅助**：对接大模型获取专业分析，辅助投资决策
- **隐私安全**：所有数据本地存储，不上传云端，保护投资隐私

### 1.4 项目范围
- **包含**：股票池管理、股票筛选、每日收盘后智能分析、持仓盈亏管理、大模型AI分析
- **不包含**：盘中实时行情推送、在线交易、社区交流、付费资讯

---

## 2. 用户画像与核心诉求
### 2.1 用户画像
- **身份**：普通个人散户，非专业投资者，有稳定全职工作
- **投资风格**：中长线价值投资/波段操作，交易频率低（每周1-2次）
- **使用习惯**：每日仅在收盘后（15:30-16:00）花10-30分钟查看股票，不进行盘中交易
- **技术能力**：熟悉电脑基本操作，能使用简单的软件功能
- **核心痛点**：
  1. 现有软件广告多、启动慢，干扰工作
  2. 盘中实时推送容易分心，影响工作效率
  3. 难以快速筛选出符合自己条件的股票
  4. 持仓和盈亏统计分散，需要手动计算
  5. 缺乏专业分析能力，决策困难

### 2.2 核心诉求
1. 能够不定期快速筛选股票，建立和管理个人股票池
2. 每日收盘后自动分析股票池，达到预设条件时给出交易建议
3. 简单直观地管理持仓，自动计算盈亏和资产情况
4. 能够一键获取大模型对单只股票的专业分析
5. 应用轻量、启动快、不占用过多系统资源
6. 所有数据本地存储，保护投资隐私

---

## 3. 核心使用场景
### 场景1：不定期筛选股票
- **触发条件**：用户有空闲时间，想要寻找新的投资标的
- **用户行为**：打开应用 → 选择预设的筛选条件 → 运行筛选 → 查看结果 → 将感兴趣的股票加入股票池
- **预期结果**：1分钟内完成筛选，快速获取潜在投资标的

### 场景2：每日收盘后智能分析与提醒
- **触发条件**：每日15:30股市收盘
- **系统行为**：Rust后端自动下载当日行情数据 → 分析股票池中的所有股票 → 检查是否触发监控条件 → 生成交易建议报告 → 发送桌面通知
- **用户行为**：收到通知后打开应用 → 查看交易建议报告 → 决定是否进行交易
- **预期结果**：准确识别触发条件的股票，生成清晰的交易建议，仅发送一次通知

### 场景3：收盘后查看持仓和盈亏
- **触发条件**：用户收到每日交易提醒或主动打开应用
- **用户行为**：打开应用 → 查看持仓列表 → 查看当日盈亏和总资产 → 查看资产变化趋势
- **预期结果**：直观展示各项数据，自动计算的收益率和持仓占比准确无误

### 场景4：使用AI分析股票
- **触发条件**：用户想要了解某只股票的详细情况
- **用户行为**：选择一只股票 → 点击"AI分析"按钮 → 查看生成的分析报告
- **预期结果**：10秒内生成包含基本面、技术面、风险提示和投资建议的完整报告

---

## 4. 功能需求（按优先级排序）
### 4.1 P0：必须实现的核心功能
#### 4.1.1 股票池管理
- 支持创建、编辑、删除多个股票池分组（如：潜力股、持仓股、观察股）
- 支持手动添加/删除单只股票到股票池
- 支持批量导入/导出股票池（Excel/CSV格式）
- 股票池列表显示字段：股票代码、股票名称、最新价、涨跌幅、成交量、监控状态
- 支持按涨跌幅、价格等字段对股票池列表进行排序

#### 4.1.2 股票筛选功能
- 提供以下常用筛选条件：
  - 价格区间（如：5-20元）
  - 涨跌幅区间（如：近3日涨幅>5%、近1月跌幅>10%）
  - 成交量区间（如：今日成交量>1亿）
  - 市盈率区间（如：0-30倍）
  - 换手率区间（如：3%-10%）
- 支持保存自定义筛选条件，下次直接使用
- 支持一键将筛选结果中的股票加入指定股票池

#### 4.1.3 每日收盘后智能分析与交易建议
- 每日15:30自动下载当日A股所有股票的行情数据
- 支持为单只股票设置多个监控条件：
  - 价格条件：收盘价≥XX元（买入提醒）、收盘价≤XX元（卖出提醒）
  - 涨跌幅条件：单日涨跌幅≥XX%、近3日涨跌幅≥XX%
  - 成交量条件：单日成交量≥XX手
- 支持批量为股票池中的股票设置监控条件
- 生成每日交易建议报告，包含：股票名称、触发条件、当前价格、建议操作（买入/卖出/持有）
- 支持查看历史交易建议报告（按日期查询）
- 提醒方式：桌面弹窗通知+可选声音提醒+应用内小红点提示

#### 4.1.4 持仓与盈亏管理
- 支持手动录入每笔交易记录（买入/卖出）：股票代码、股票名称、交易数量、交易价格、交易日期
- 自动计算以下数据：
  - 单只股票：持仓数量、持仓成本、最新价、浮动盈亏、收益率、持仓占比
  - 整体：总持仓市值、总浮动盈亏、总资产、总收益率
- 持仓列表显示上述所有计算字段
- 支持查看历史交易记录，可按日期、股票代码进行筛选
- 支持删除错误的交易记录

#### 4.1.5 大模型股票分析
- 支持配置主流大模型API密钥（豆包、文心一言、通义千问）
- 用户选择单只股票后，可一键获取AI分析报告
- 分析报告内容：基本面分析、技术面分析、近期热点、风险提示、投资建议
- 支持保存AI分析报告到本地
- 支持查看历史AI分析报告

### 4.2 P1：建议实现的重要功能
#### 4.2.1 基础行情展示
- 股票详情页显示日K线图（包含MA均线和成交量）
- 显示股票基本信息：市盈率、市净率、总市值、流通市值、换手率
- 支持切换K线周期（日K/周K/月K）

#### 4.2.2 资产统计与分析
- 资产净值曲线：展示每日资产变化趋势
- 持仓分布饼图：按行业、按股票展示持仓占比
- 月度收益统计：展示每月盈亏和收益率
- 单只股票收益排名：按收益率对持仓股票进行排序

#### 4.2.3 数据导出功能
- 支持导出持仓数据、交易记录、资产统计为Excel/CSV格式
- 支持导出股票池、筛选结果和交易建议报告

### 4.3 P2：未来规划的扩展功能
- 支持通过Excel导入券商交易记录
- 支持多账户管理（A股/港股/美股）
- 支持自定义技术指标
- 支持交易笔记功能，记录买卖理由和心得
- 支持深色/浅色主题切换
- 支持常用操作快捷键

---

## 5. 非功能需求
### 5.1 性能需求
- 应用冷启动时间：≤1.5秒
- 筛选1000只股票的时间：≤3秒
- 每日收盘后数据更新+分析时间：≤20秒
- 后台静默运行内存占用：≤40MB
- 连续运行72小时无明显卡顿和内存泄漏

### 5.2 体验需求
- 界面简洁清爽，无任何广告
- 核心操作流程不超过3步
- 点击按钮响应时间：≤200毫秒
- 支持窗口最小化到系统托盘，后台静默运行
- **非打扰式设计**：除每日收盘后一次通知外，不推送任何其他消息

### 5.3 安全与隐私需求
- 所有用户数据（持仓、交易记录、股票池、交易建议、AI分析报告）仅存储在本地SQLite数据库，不上传任何云端
- 支持设置应用启动密码，保护隐私
- 大模型API密钥使用AES-256-GCM加密存储
- 严格的前后端隔离，前端无法直接访问文件系统和数据库
- 不收集任何用户个人信息和投资数据

### 5.4 兼容性需求
- 支持Windows 10 1809及以上版本（自带Edge WebView2）
- 支持1080P、2K、4K不同分辨率的显示器
- 单文件部署，无需安装，双击即可运行

---

## 6. 验收标准
### 6.1 核心功能验收
| 功能模块 | 验收项 | 验收标准 |
|---|---|---|
| 股票池管理 | 创建分组 | 能成功创建3个以上股票池分组 |
| | 添加删除股票 | 能手动添加/删除单只股票，批量导入导出股票池 |
| | 列表显示 | 股票池列表显示正确的最新价和涨跌幅 |
| 股票筛选 | 保存条件 | 能设置并保存5个以上自定义筛选条件 |
| | 筛选结果 | 筛选结果准确，符合设置的条件 |
| | 加入股票池 | 能一键将筛选结果加入指定股票池 |
| 每日智能分析 | 自动运行 | 每日15:30自动下载行情数据并分析股票池 |
| | 条件设置 | 能为单只股票设置多个监控条件 |
| | 提醒功能 | 股票触发条件时，生成交易建议报告并弹出桌面通知 |
| | 历史记录 | 能查看历史交易建议报告 |
| 持仓管理 | 录入交易 | 能成功录入买入/卖出交易记录 |
| | 盈亏计算 | 自动计算的持仓成本、盈亏和收益率准确无误 |
| | 历史记录 | 能查看并筛选历史交易记录 |
| 大模型分析 | 密钥配置 | 能成功配置大模型API密钥 |
| | 生成报告 | 点击AI分析按钮后，10秒内生成完整分析报告 |
| | 历史记录 | 能查看历史AI分析报告 |

### 6.2 非功能验收
- 应用冷启动时间≤1.5秒
- 后台静默运行内存占用≤40MB
- 所有数据仅存储在本地，不上传云端
- 支持设置应用启动密码
- 除每日收盘后一次通知外，无其他任何推送消息
- 生成的可执行文件≤12MB，无需安装即可运行

---

## 7. Tauri 2.0 专属技术实现方案
### 7.1 整体架构
```
┌─────────────────────────────────────────┐
│ 前端层（React 18 + TypeScript + Vite）   │
│ - UI组件与页面布局（Ant Design）         │
│ - ECharts图表展示（K线、统计图表）       │
│ - Zustand状态管理                       │
│ - 通过Tauri 2.0 IPC调用后端命令         │
└───────────────────┬─────────────────────┘
                    │ IPC通信（类型安全）
┌───────────────────▼─────────────────────┐
│ 后端层（Rust 1.78+ + Tauri 2.0 正式版）  │
│ ┌─────────────┐ ┌─────────────┐ ┌───────┐
│ │ 行情获取模块 │ │ 数据分析模块 │ │ 定时  │
│ │ （reqwest） │ │ （筛选/计算） │ │ 任务  │
│ └─────────────┘ └─────────────┘ └───────┘
│ ┌─────────────┐ ┌─────────────┐ ┌───────┐
│ │ 大模型调用   │ │ 数据持久化   │ │ 安全  │
│ │ 模块         │ │ （sqlx）     │ │ 模块  │
│ └─────────────┘ └─────────────┘ └───────┘
│ - 系统托盘、桌面通知、窗口管理          │
└───────────────────┬─────────────────────┘
                    │
┌───────────────────▼─────────────────────┐
│ 本地存储层（SQLite 3.45+）               │
│ - 股票池、交易记录、持仓数据            │
│ - 监控条件、交易建议、AI分析报告        │
│ - 系统配置（加密存储API密钥）           │
└─────────────────────────────────────────┘
```

### 7.2 技术栈详情（Tauri 2.0 专属）
| 层级 | 技术选型 | 说明 |
|---|---|---|
| 前端 | React 18 + TypeScript + Vite 5 | 快速构建现代化前端应用 |
| 前端UI | Ant Design 5.x | 提供丰富的组件库，快速开发界面 |
| 图表 | ECharts 5.5.x | 绘制K线图、资产曲线、持仓分布饼图 |
| 状态管理 | Zustand 4.x | 轻量高效的前端状态管理 |
| 后端 | Rust 1.78+ + Tauri 2.0 正式版 | 高性能、内存安全的后端框架 |
| 数据库 | SQLite 3.45+ + sqlx 0.7 | 轻量嵌入式数据库，编译时类型检查 |
| 定时任务 | tokio-cron-scheduler 0.10 | 实现每日收盘后自动分析任务 |
| HTTP客户端 | reqwest 0.12 | 高性能异步HTTP客户端 |
| 加密 | aes-gcm 0.10 + argon2 0.5 | 加密存储API密钥和启动密码 |
| Excel处理 | calamine 0.24 | 读写Excel/CSV文件 |
| 打包工具 | Tauri CLI 2.0 | 一键生成Windows单文件可执行程序 |

### 7.3 Tauri 2.0 核心特性应用
#### 7.3.1 全新的IPC命令系统（Tauri 2.0 专属）
Tauri 2.0 引入了更简洁、更类型安全的IPC命令系统：
```rust
// 后端Rust代码（Tauri 2.0 新语法）
#[tauri::command]
async fn filter_stocks(conditions: FilterConditions) -> Result<Vec<Stock>, String> {
    // 筛选逻辑
    Ok(stocks)
}

#[tauri::command]
async fn get_stock_pools() -> Result<Vec<StockPool>, String> {
    // 查询数据库
    Ok(pools)
}

// 注册命令
fn main() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![
            filter_stocks,
            get_stock_pools,
            add_stock_to_pool,
            set_monitor_condition,
            add_transaction,
            get_positions,
            analyze_stock_with_ai
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

```typescript
// 前端TypeScript代码（Tauri 2.0 新API）
import { invoke } from '@tauri-apps/api/core'

// 类型安全的IPC调用
const stocks = await invoke<Stock[]>('filter_stocks', { conditions })
const pools = await invoke<StockPool[]>('get_stock_pools')
```

#### 7.3.2 全新的权限系统（Tauri 2.0 专属）
Tauri 2.0 采用了更精细的权限管理系统，在`tauri.conf.json`中配置：
```json
{
  "tauri": {
    "permissions": [
      "core:default",
      "window:default",
      "notification:default",
      {
        "identifier": "fs:default",
        "allow": [{ "path": "$APPLOCALDATA/**" }]
      },
      {
        "identifier": "http:default",
        "allow": [
          "https://api.tushare.pro/*",
          "https://api.doubao.com/*",
          "https://aip.baidubce.com/*"
        ]
      }
    ],
    "security": {
      "csp": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' https://api.tushare.pro https://api.doubao.com https://aip.baidubce.com"
    }
  }
}
```

#### 7.3.3 数据库设计
```sql
-- 股票池表
CREATE TABLE stock_pools (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 股票表
CREATE TABLE stocks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT NOT NULL,
    name TEXT NOT NULL,
    pool_id INTEGER NOT NULL,
    latest_price REAL,
    change_percent REAL,
    volume INTEGER,
    monitored BOOLEAN DEFAULT 1,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (pool_id) REFERENCES stock_pools(id) ON DELETE CASCADE
);

-- 监控条件表
CREATE TABLE monitor_conditions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    stock_id INTEGER NOT NULL,
    condition_type TEXT NOT NULL, -- price_up, price_down, change_percent, volume
    threshold REAL NOT NULL,
    action TEXT NOT NULL, -- buy, sell, hold
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (stock_id) REFERENCES stocks(id) ON DELETE CASCADE
);

-- 交易记录表
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    stock_code TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    type TEXT NOT NULL, -- buy, sell
    quantity INTEGER NOT NULL,
    price REAL NOT NULL,
    amount REAL NOT NULL,
    transaction_date DATETIME NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 持仓表
CREATE TABLE positions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    stock_code TEXT NOT NULL UNIQUE,
    stock_name TEXT NOT NULL,
    quantity INTEGER NOT NULL,
    cost_price REAL NOT NULL,
    latest_price REAL,
    floating_profit REAL,
    profit_rate REAL,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 交易建议表
CREATE TABLE trade_advices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    stock_code TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    condition TEXT NOT NULL,
    current_price REAL NOT NULL,
    advice TEXT NOT NULL, -- buy, sell, hold
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- AI分析报告表
CREATE TABLE ai_analyses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    stock_code TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    content TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 系统配置表
CREATE TABLE system_configs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT NOT NULL UNIQUE,
    value TEXT NOT NULL, -- 加密存储
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 7.3.4 每日收盘后定时任务
```rust
use tokio_cron_scheduler::{Job, JobScheduler};
use tauri::AppHandle;

// 在Tauri应用启动时注册定时任务
fn setup_scheduler(app_handle: AppHandle) {
    tauri::async_runtime::spawn(async move {
        let mut scheduler = JobScheduler::new().await.unwrap();
        
        // 每日15:30执行收盘后分析任务
        let job = Job::new("0 30 15 * * *", move |_uuid, _l| {
            let app_handle = app_handle.clone();
            tauri::async_runtime::spawn(async move {
                // 1. 下载当日A股行情数据
                let market_data = match fetch_daily_astock_data().await {
                    Ok(data) => data,
                    Err(e) => {
                        eprintln!("Failed to fetch market data: {}", e);
                        return;
                    }
                };
                
                // 2. 更新股票池股票价格
                if let Err(e) = update_stock_prices(&market_data).await {
                    eprintln!("Failed to update stock prices: {}", e);
                    return;
                }
                
                // 3. 检查所有监控条件
                let triggered_stocks = match check_all_monitor_conditions(&market_data).await {
                    Ok(stocks) => stocks,
                    Err(e) => {
                        eprintln!("Failed to check monitor conditions: {}", e);
                        return;
                    }
                };
                
                // 4. 生成交易建议报告
                if !triggered_stocks.is_empty() {
                    if let Err(e) = generate_trade_advice_report(&triggered_stocks).await {
                        eprintln!("Failed to generate trade advice: {}", e);
                        return;
                    }
                    
                    // 5. 发送桌面通知（Tauri 2.0 新API）
                    use tauri::api::notification::Notification;
                    Notification::new(&app_handle.config().identifier)
                        .title("股票交易提醒")
                        .body(&format!("今日有{}只股票触发交易条件", triggered_stocks.len()))
                        .show(&app_handle)
                        .unwrap();
                }
            });
        }).unwrap();
        
        scheduler.add(job).await.unwrap();
        scheduler.start().await.unwrap();
    });
}
```

#### 7.3.5 系统托盘实现（Tauri 2.0 专属）
```rust
use tauri::SystemTray;
use tauri::SystemTrayMenu;
use tauri::SystemTrayMenuItem;
use tauri::CustomMenuItem;

fn main() {
    // 创建系统托盘菜单
    let tray_menu = SystemTrayMenu::new()
        .add_item(CustomMenuItem::new("show", "打开应用"))
        .add_native_item(SystemTrayMenuItem::Separator)
        .add_item(CustomMenuItem::new("quit", "退出"));
    
    // 创建系统托盘
    let system_tray = SystemTray::new()
        .with_menu(tray_menu);
    
    tauri::Builder::default()
        .system_tray(system_tray)
        .on_system_tray_event(|app, event| match event {
            tauri::SystemTrayEvent::MenuItemClick { id, .. } => {
                match id.as_str() {
                    "show" => {
                        let window = app.get_window("main").unwrap();
                        window.show().unwrap();
                        window.set_focus().unwrap();
                    }
                    "quit" => {
                        app.exit(0);
                    }
                    _ => {}
                }
            }
            _ => {}
        })
        .invoke_handler(tauri::generate_handler![/* 命令列表 */])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

### 7.4 开发环境搭建（Tauri 2.0 专属）
1. 安装Rust工具链：
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```
2. 安装Node.js 20+和pnpm
3. 创建Tauri 2.0项目：
   ```bash
   pnpm create tauri-app@latest --template react-ts
   ```
4. 安装依赖：
   ```bash
   pnpm install
   ```
5. 启动开发服务器：
   ```bash
   pnpm tauri dev
   ```

### 7.5 打包部署（Tauri 2.0 专属）
- 构建生产版本：
  ```bash
  pnpm tauri build
  ```
- 输出文件：
  - Windows单文件可执行程序：`src-tauri/target/release/stock-investment-manager.exe`（约8-12MB）
  - Windows安装包：`src-tauri/target/release/bundle/msi/stock-investment-manager_1.0.0_x64_en-US.msi`
- 部署方式：直接复制可执行文件到任意Windows 10+电脑，双击即可运行，无需安装任何依赖

---

## 8. 风险与应对措施
| 风险点 | 影响程度 | 应对措施 |
|---|---|---|
| 免费行情API不稳定 | 中 | 同时对接2-3个免费行情API（Tushare、新浪财经、东方财富），主API失败时自动切换备用API |
| 大模型API调用失败 | 中 | 添加错误处理和重试机制，给用户明确的失败提示，支持手动重新生成 |
| 本地数据丢失 | 高 | 支持手动备份和恢复SQLite数据库功能，提示用户定期备份 |
| 应用被杀毒软件误报 | 中 | 为可执行文件添加数字签名，在文档中说明添加信任的方法 |
| WebView兼容性问题 | 低 | 要求用户系统为Windows 10 1809+，自带Edge WebView2 |

---

## 9. 附录
### 9.1 术语定义
- **股票池**：用户自定义的股票分组，用于跟踪和管理感兴趣的股票
- **监控条件**：用户为股票设置的触发提醒的条件
- **交易建议**：系统根据监控条件生成的买入/卖出/持有建议
- **浮动盈亏**：持仓股票当前市值与持仓成本之间的差额
- **IPC**：进程间通信，前端与后端交互的方式

### 9.2 文档修订历史
| 版本号 | 修订日期 | 修订内容 |
|---|---|---|
| V1.0 | 2026-05-15 | 初始版本 |
| V1.1 | 2026-05-15 | 根据用户反馈调整核心使用场景 |
| V1.2 | 2026-05-15 | 适配Trae全栈框架 |
| V1.3 | 2026-05-15 | 最终版，完善结构和内容 |
| V1.4 | 2026-05-15 | 调整为Rust+WebView架构 |
| V1.5 | 2026-05-15 | 升级为Tauri 2.0正式版专属方案 |

需要我基于这份Tauri 2.0专属需求说明书，为你生成**完整的项目初始化代码**（包含数据库模型、核心IPC命令、前端页面骨架和配置文件）吗？